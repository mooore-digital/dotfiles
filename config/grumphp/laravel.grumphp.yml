parameters:
  git_dir: .
  bin_dir: vendor/bin
  tasks:
    phpcs:
      standard: PSR2
      whitelist_patterns:
        - app
      ignore_patterns:
        - src/Migrations
        - src/Kernel.php
